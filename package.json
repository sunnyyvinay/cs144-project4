{
    "name": "mandelbrot-explorer",
    "version": "1.0.0",
    "description": "Mandelbrot fractal explorer using WebAssembly",
    "main": "server.js",
    "scripts": {
        "start": "node server.js",
        "build:wasm": "cd wasm && make",
        "clean:wasm": "cd wasm && make clean",
        "prepare-dist": "rm -rf node_modules .claude && rm -f package-lock.json"
    },
    "dependencies": {
        "express": "^5.2.1"
    }
}
